#include <stdio.h>
#include <stdlib.h>
#include <dos.h>
#include <conio.h>
#include <time.h>
#include <string.h>

int main ()
{

    char senha1[21] = "ser,estar",senha2[21],deposito[26];
    printf("[1] Digite a traducao do verbo BE:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "aguentar",senha2[21],deposito[26];
    printf("[2] Digite a traducao do verbo BEAR:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "bater,espancar",senha2[21],deposito[26];
    printf("[3] Digite a traducao do verbo BEAT:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "ficar,tornar",senha2[21],deposito[26];
    printf("[4] Digite a traducao do verbo BECOME:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "comecar",senha2[21],deposito[26];
    printf("[5] Digite a traducao do verbo BEGIN:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "curvar,dobrar",senha2[21],deposito[26];
    printf("[6] Digite a traducao do verbo BEND:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "morder",senha2[21],deposito[26];
    printf("[7] Digite a traducao do verbo BITE:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "sangrar",senha2[21],deposito[26];
    printf("[8] Digite a traducao do verbo BLEED:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "soprar",senha2[21],deposito[26];
    printf("[9] Digite a traducao do verbo BLOW:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "quebrar",senha2[21],deposito[26];
    printf("[10] Digite a traducao do verbo BREAK:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "trazer",senha2[21],deposito[26];
    printf("[11] Digite a traducao do verbo BRING:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "construir",senha2[21],deposito[26];
    printf("[12] Digite a traducao do verbo BUILD:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "estourar,explodir",senha2[21],deposito[26];
    printf("[13] Digite a traducao do verbo BURST:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "comprar",senha2[21],deposito[26];
    printf("[14] Digite a traducao do verbo BUY:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    if (strcmp (senha1,senha2) == 0 ) {
    char senha1[21] = "pegar",senha2[21],deposito[26];
    printf("[15] Digite a traducao do verbo CATCH:\n");
    fgets(deposito, sizeof(deposito), stdin);
    sscanf(deposito, "%s", &senha2);
    system("cls");
    }}}}}}}}}}}}}}}
